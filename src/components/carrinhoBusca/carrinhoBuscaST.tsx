import styled from "styled-components";

export const icons = styled.div`
  display: flex;
  justify-content: end;
  gap: 0.5rem;
  font-size: 1.5rem;
  font-weight: 100;
  color: rgba(0, 0, 0, 0.8);
`;
